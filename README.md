# Discord Forex Ticker

A Discord bot that displays the current price of a currency pair.

## Usage

Copy the `.env.example` file to `.env` and fill in the required values.

`pip install -r requirements.txt`

`python main.py`